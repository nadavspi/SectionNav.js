{
  "name": "section-nav",
  "version": "1.0.2",
  "description": "Renders a menu of anchor links for navigating between sections of a page.",
  "main": "lib/index.js",
  "files": [
    "lib",
    "src",
    "*.md"
  ],
  "scripts": {
    "start": "npm run watch",
    "server": "http-server .",
    "watch": "babel src -d lib --watch --source-maps inline",
    "build": "mkdir -p lib && babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nadavspi/SectionNav.js.git"
  },
  "author": "Nadav Spiegelman <me@nadav.name>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "http-server": "^0.9.0"
  },
  "dependencies": {}
}
